<Window x:Class="IntronFileController.Views.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="ToolWindow"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Title="Carregando...">
    <Border Padding="16">
        <StackPanel>
            <TextBlock x:Name="MessageText" Text="Carregando..." FontSize="14" Margin="0,0,0,8" />
            <ProgressBar IsIndeterminate="True" Width="300" Height="16" />
        </StackPanel>
    </Border>
</Window>