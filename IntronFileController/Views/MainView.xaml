<Window
    x:Class="IntronFileController.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:IntronFileController.Views"
    xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
    xmlns:materialDesignColors="clr-namespace:MaterialDesignColors;assembly=MaterialDesignColors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Intron File Editor"
    Width="1000"
    Height="700"
    MinWidth="1000"
    MinHeight="700"
    Background="{DynamicResource MaterialDesignPaper}"
    Foreground="{DynamicResource MaterialDesignBody}"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Visibility="{Binding NavigateBackVisibility}">
                <Button 
                        Command="{Binding NavigateBackCommand}"
                        Style="{DynamicResource MaterialDesignIconForegroundButton}"
                        DockPanel.Dock="Right"
                        Height="30"
                        Width="30">
                    <materialDesign:PackIcon Kind="ArrowBack" />
                </Button>
                <TextBlock Margin="5, 0, 0 ,0" Text="Cancelar e voltar" VerticalAlignment="Center" Style="{DynamicResource MaterialDesignBodyLargeTextBlock}"/>
            </StackPanel>
            <TextBlock Grid.Column="0"
                       Grid.ColumnSpan="3"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Style="{DynamicResource MaterialDesignTitleLargeTextBlock}"
                       Text="{Binding TitleText}" />
            <Border
                Grid.Column="2"
                Width="30"
                Height="30"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="Transparent"
                MouseDown="PackIcon_MouseDown">
                <materialDesign:PackIcon
                    Width="30"
                    Height="30"
                    Kind="{Binding CurrentThemeIcon}" />
            </Border>

        </Grid>
        <UserControl
            Grid.Row="1"
            Margin="0,12,0,0"
            Content="{Binding CurrentUC}" />
    </Grid>
</Window>
